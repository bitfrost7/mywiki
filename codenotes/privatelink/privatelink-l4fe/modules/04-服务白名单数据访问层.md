# 模块 04：服务白名单数据访问层 — Service Whitelist Data Access

## 概述

该模块对应图分析 **Community 3「Service Whitelist Data Access」**（70 个节点，内聚度 0.06），提供 `t_service_whitelist` 表的 ORM 数据访问层代码。由 GORM gen 自动生成。

## 源文件

- **`mygorm/db/t_service_whitelist.gen.go`** — DAO 实现（自动生成，396 行）
- **`mygorm/model/t_service_whitelist.gen.go`** — 数据模型（自动生成，21 行）

## 核心结构体

### TServiceWhitelist 模型 (`model/t_service_whitelist.gen.go:10-16`)

映射 `t_service_whitelist` 表，字段包括：

| 字段 | 类型 | 说明 |
|------|------|------|
| `ID` | `uint32` | 自增主键 |
| `ServiceID` | `string` | 关联的服务 ID |
| `AccountID` | `uint32` | 白名单账户 ID |
| `InsertTime` | `uint32` | 插入时间 |
| `DeleteTime` | `uint32` | 删除时间（0=未删除） |

## 使用状态

**当前代码中未使用该 DAO**。虽然代码生成了 `tServiceWhitelist` DAO 和模型，但在 `api/grpc_api.go` 和 `model/privatelink.go` 中都没有引用。它保留在 `Query` 结构中 (`db/gen.go:23`)，作为基础设施预留。

可能的用途：Privatelink 服务白名单功能（限制哪些账户可以连接该服务）尚待实现或已在其他服务中处理。
