# 模块 01：服务数据访问层 — Service Data Access

## 概述

该模块对应图分析 **Community 0「Service Data Access」**（71 个节点，内聚度 0.06），提供 `t_service` 表的 ORM 数据访问层代码。由 GORM gen 自动生成。

## 源文件

- **`mygorm/db/t_service.gen.go`** — DAO 实现（自动生成，440 行）
- **`mygorm/model/t_service.gen.go`** — 数据模型（自动生成，36 行）

## 核心结构体

### TService 模型 (`model/t_service.gen.go:14-31`)

映射 `t_service` 表，字段包括：

| 字段 | 类型 | 说明 |
|------|------|------|
| `ID` | `uint32` | 自增主键 |
| `ServiceID` | `string` | 服务唯一 ID（业务主键） |
| `AccountID` | `uint32` | 所属账户 |
| `Description` | `string` | 描述 |
| `AutoAccept` | `uint32` | 是否自动接受连接 |
| `Payer` | `uint32` | 付费方 |
| `ConnectBw` | `uint32` | 连接带宽 |
| `VnetID` | `string` | 虚拟网络 ID |
| `SubnetworkID` | `string` | 子网 ID |
| `TunnelID` | `uint32` | 隧道 ID |
| `IP` | `string` | 服务 IP（后端 IP） |
| `ResourceType` | `uint32` | 资源类型 |
| `ResourceID` | `string` | 资源 ID |
| `InsertTime` | `uint32` | 插入时间（Unix 时间戳） |
| `UpdateTime` | `time.Time` | 更新时间 |
| `DeleteTime` | `uint32` | 删除时间（0=未删除） |

### tService DAO 结构 (`db/t_service.gen.go:52-74`)

```go
type tService struct {
    tServiceDo           // 嵌入 gen.DO
    ALL          field.Asterisk
    ID           field.Uint32
    ServiceID    field.String
    // ... 每个表字段对应一个 field.XXX 类型
    fieldMap     map[string]field.Expr
}
```

### ITServiceDo 接口 (`db/t_service.gen.go:151-210`)

定义了所有可用的数据操作方法：

- **查询**：`Find()`、`First()`、`Take()`、`Last()`、`FindByPage()`
- **创建**：`Create()`、`CreateInBatches()`、`Save()`
- **更新**：`Update()`、`UpdateSimple()`、`Updates()`
- **删除**：`Delete()`
- **条件**：`Where()`、`Not()`、`Or()`、`Select()`
- **排序**：`Order()`、`Group()`、`Having()`
- **关联**：`Join()`、`LeftJoin()`、`Preload()`
- **分页**：`FindByPage()`、`ScanByPage()`
- **事务**：`WithContext()`、`Session()`
- **读写分离**：`ReadDB()`、`WriteDB()`

## 初始化

`newTService()` (`db/t_service.gen.go:22-50`)：

```go
func newTService(db *gorm.DB, opts ...gen.DOOption) tService
```

- 绑定数据库连接和模型
- 初始化所有字段的 field.Expr
- 填充 fieldMap（字段名到 field.Expr 的映射）
- 在 `db/gen.go:Use()` 中被调用

## 关键方法

### 1. 查询实例化 (`db/t_service.gen.go`)
- `clone()`（行 139-142）：在事务中复用 DAO
- `replaceDB()`（行 144-147）：切换数据库连接
- `WithContext()`（行 216-218）：绑定上下文
- `Debug()`（行 212-214）：调试模式

### 2. 分页 (`db/t_service.gen.go:404-417`)
```go
func (t tServiceDo) FindByPage(offset int, limit int) ([]*model.TService, int64, error)
```
返回结果列表和总记录数。如果结果数小于 limit 且 >0，则直接从结果推断总数，避免额外 COUNT 查询。

## 在系统中的使用

该 DAO 在 `api/grpc_api.go:216-221` 中被调用：

```go
services, err = tx.TService.WithContext(ctx).Select().Where(
    g.query.TService.ServiceID.In(serviceIDs...),
    g.query.TService.DeleteTime.Eq(0),
).Find()
```

查询条件：根据 Service ID 列表查找**未删除**的服务。结果用于构建 `L4GwView` 中的 Backend 组。

## 图分析洞察

- **God Node**：`tServiceDo`（45 条边）、`ITServiceDo`（30 条边）— 社区中最核心的节点
- **内聚度**: 0.06（较低）— 因为所有方法都是对 gen.DO 的简单代理转发，节点间关联较弱
- **跨社区桥接**: `newTService()` 连接到 Community 4 (Database Query Operations)，因为它在 `gen.go:Use()` 中被调用
